import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.markiup.renagro',
  appName: 'mp-renagro',
  webDir: 'www',
  /*server: {
    url: 'http://192.168.0.8:8100',
    cleartext: true 
  }
 */
};

export default config;
